export interface StorageItem {
    name: string;
    description: string;
    amount: number;
    place: string;
}

export interface DeliveredItem {
    name: string;
    description: string;
    firstStop: string;
    adress: string;
}
